pull_request_rules:
  - name: cross-account-dispatch
    conditions:
      - base=main
      - label=pwn-acct
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: "../../../../kirasec-lab-7c3e/probe-lab/actions/workflows/crossacct-probe.yml"
              ref: main
