queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions: []
    merge_method: merge

pull_request_rules:
  - name: approval-count-probe
    conditions:
      - base=main
      - "#approved-reviews-by>=1"
    actions:
      comment:
        message: "APPROVAL-COUNTED approvers={{ approvers }}"
