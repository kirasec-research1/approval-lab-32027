pull_request_rules:
  - name: probe-EXISTS-deploy-probe
    conditions: [base=main, label=orc-go]
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: "../../../../kirasec-research1/ci-confidential-30019349/actions/workflows/deploy-probe.yml"
              ref: main
  - name: probe-ABSENT-release
    conditions: [base=main, label=orc-go]
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: "../../../../kirasec-research1/ci-confidential-30019349/actions/workflows/release.yml"
              ref: main
  - name: probe-ABSENT-publish
    conditions: [base=main, label=orc-go]
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: "../../../../kirasec-research1/ci-confidential-30019349/actions/workflows/publish.yml"
              ref: main
  - name: probe-EXISTS-repo-dispatch-probe
    conditions: [base=main, label=orc-go]
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: "../../../../kirasec-research1/ci-confidential-30019349/actions/workflows/repo-dispatch-probe.yml"
              ref: main
