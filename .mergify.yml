pull_request_rules:
  - name: botacct-probe
    conditions:
      - base=main
    actions:
      comment:
        message: "BOTACCT-PROBE-77120 posted via bot_account"
        bot_account: kirasec-research1